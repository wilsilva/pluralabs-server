sudo: false
language: node_js
node_js:
  - "11"
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version version-number
  - export PATH="$HOME/.yarn/bin:$PATH"
services:
  - mongodb
cache:
  yarn: true
  directories:
    - node_modules
script:
  - yarn test
